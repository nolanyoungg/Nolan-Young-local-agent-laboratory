# Delivery safety

Approved sends require a configured provider, sender, recipient, and API key. Use idempotency records. Attach the generated `blog.md`, preserve the preview on failure, and update the tracker only after delivery succeeds.
