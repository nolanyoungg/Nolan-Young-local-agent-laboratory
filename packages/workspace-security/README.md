# `@laboratory/workspace-security`

Canonical workspace roots, path policies, sensitive-path defaults, ignore matching, traversal and symlink-escape rejection, and exclusive mutation locks. Write permission is distinct from read permission and no deletion capability is provided.
