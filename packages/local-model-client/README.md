# `@laboratory/local-model-client`

Provider-neutral local model interface with Ollama and deterministic mock implementations. Ollama requests are bounded by timeouts and retries, require no API key, and never fall back to a hosted provider.
