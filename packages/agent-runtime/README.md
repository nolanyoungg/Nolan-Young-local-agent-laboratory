# `@laboratory/agent-runtime`

Focused-agent execution with structured response parsing, bounded malformed-output retries, explicit tool permissions, step and repetition limits, deterministic context budgeting, local tracing, and output-schema validation.
