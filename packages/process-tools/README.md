# `@laboratory/process-tools`

Application-owned command allowlists, shell-free one-shot and watcher execution, bounded stdout/stderr tails, status stores, timeouts, early-exit detection, signal handling, and deterministic cleanup. Models can select identifiers only; they cannot construct commands.
