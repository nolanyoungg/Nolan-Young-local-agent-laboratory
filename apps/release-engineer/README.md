# Release Engineer

Runs mandatory deterministic checks, optionally performs bounded policy-preserving repairs, builds a local ZIP, inspects every archive entry before extraction, validates a temporary extraction, enforces archive size/content rules, generates SHA-256, and writes release reports. It never publishes, tags, commits, or pushes.
