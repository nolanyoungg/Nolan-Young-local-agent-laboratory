# Build Assistant

Runs an application-owned one-shot build or watcher, captures bounded logs, diagnoses failures with a read-only agent, performs up to a configured number of focused repairs, reruns deterministic checks, reviews changes read-only, stops watchers in `finally`, and preserves reports on failure.
