# Getting started

Install Node 22 LTS and Ollama, run `ollama pull qwen2.5-coder:14b`, then `npm ci` and `npm run validate`. Check local readiness with `npm run check:ollama`. Run an application's `--help` before using it. Begin with a copied example and plan-only or dry-run mode.
