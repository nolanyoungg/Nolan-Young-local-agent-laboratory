# Adding an application

Add one `apps/<name>` workspace with a narrow responsibility, CLI, typed configuration, prompts, policies, workflow, reports, and tests. Reuse internal packages. Register only required tools, acquire mutation locks in `try/finally`, persist deterministic results, and add the project reference and root command. Do not broaden existing applications.
