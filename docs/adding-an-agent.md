# Adding an agent

Define a focused identity, instructions, explicit tool list, maximum steps, model configuration, and structured final output. Prompts must require evidence and prohibit invention, but code must independently enforce the same permissions. Prefer read-only roles and use separate planner, mutator, and reviewer boundaries when appropriate.
